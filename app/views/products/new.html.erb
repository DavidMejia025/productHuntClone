<!-- <div class = "container">
	<div class = "row">
		<div class="col-sm-6 col-sm-offset-3">
			<h1>Publish your product</h1>
			<%= form_for @product do |f| %>
				<div class= "form-group"> 
					<%= f.text_field :name, class:"form-control input-lg", placeholder: "fill with the product name" %>
				</div>
				<div class= "form-group"> 
					<%= f.text_field :url, class:"form-control input-lg", placeholder: "fill with the url of your product " %>
				</div>
				<div class= "form-group"> 
					<%= f.text_area :description, class:"form-control input-lg", placeholder: "add a short description" %>
				</div>
				<div>
					<%= f.submit "Send product", class: "btn btn-primary btn-lg btn-block" %>
				</div>
				<% end %>
		</div>
	</div>
</div> -->

<%= render 'form' %>